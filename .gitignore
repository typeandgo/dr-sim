node_modules/
dist/
coverage/
*.zip
.DS_Store

# Eklenti imza anahtarı ve paketlenmiş crx — asla commit'lenmez
dist.crx
dist.pem

# Kişisel Claude Code ayarları (proje ayarları .claude/settings.json'da)
.claude/settings.local.json

# Planlama ve tasarım dokümanları — yerelde tutulur, repoda yayınlanmaz
dr-sim-extension/

# Senaryo preset'leri: eklenti paketine hiç girmiyorlar (src/presets/'i hiçbir kaynak
# import etmiyor), yalnızca ekip içi paylaşım için üretiliyorlar. Public repoda gerçek
# bir ürünün endpoint yüzeyini taşıdıkları için takipten çıkarıldılar; dosyalar yerelde
# durur ve `npm run preset` ile her an yeniden üretilebilir.
src/presets/*.json

# SDD çalışma alanı — plan başına scratch, depoya girmez
.superpowers/
